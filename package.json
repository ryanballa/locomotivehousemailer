{
  "name": "locomotivehousemailer",
  "version": "0.1.0",
  "description": "Email service worker for Locomotive House email queue",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "test": "vitest"
  },
  "dependencies": {
    "@clerk/backend": "^2.23.2",
    "hono": "^4.3.11",
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.0.4",
    "vitest": "1.3.0",
    "wrangler": "^4.31.0"
  }
}
